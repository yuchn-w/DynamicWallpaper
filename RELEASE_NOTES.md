## 動態壁紙 0.8.1

第一個可直接下載的公開測試版。

下載 `DynamicWallpaper-0.8.1-macOS-arm64.zip`，解壓縮後即可取得「動態壁紙.app」。

- 適用 Apple Silicon Mac
- 需要 macOS 14 或更新版本
- 不包含任何桌布影片或 macOS 系統音效
- App 只會在使用者自己的 Mac 上讀取系統已有的環境音
- 使用本機臨時簽章，尚未經 Apple 公證
- 第一次開啟若顯示無法確認開發者，請在 Finder 對 App 按右鍵並選擇「打開」

另外提供 SHA-256 校驗檔，供使用者驗證下載內容。

---

## Dynamic Wallpaper 0.8.1

The first public downloadable test build.

Download `DynamicWallpaper-0.8.1-macOS-arm64.zip` and extract it to get `動態壁紙.app`.

- Supports Apple Silicon Macs
- Requires macOS 14 or later
- Includes no wallpaper videos or macOS system audio files
- Reads ambient sounds only from system resources already present on the user’s Mac
- Ad-hoc signed and not yet notarized by Apple
- If macOS cannot verify the developer on first launch, Control-click the app in Finder, choose **Open**, and confirm the prompt

A SHA-256 checksum file is included for download verification.
